// please, make appropriate changes in scripts/process-rempl-bundle.js when update this assignment

export default `window.rempl=(${rempl.toString()})();`; //require('fs').readFileSync(__dirname + '/../dist/rempl.js', 'utf8');
